const eng = 72;
const math=44;
const testname = 'Mid-Sem';
const username = 'Awal';

const res = 'Got  a total marks '+(eng+math)+' in ' + testname + ' And name of student is '+username;

const res2 = ` ${username} Got total marks = ${eng + math} in ${testname}`;


console.log(res2);